apiVersion: v1
kind: Service
metadata:
  # Service (ClusterIP) expondo a sua aplicacao
  name: myservice-api
  labels:
    app: myservice-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8081
      protocol: TCP
  selector:
    app: myservice-api
