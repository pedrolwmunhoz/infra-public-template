apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rate-limit-{FRONT_NAME}
  namespace: default
spec:
  rateLimit:
    average: 100
    burst: 200
    period: 1m
    sourceCriterion:
      ipStrategy:
        depth: 1
